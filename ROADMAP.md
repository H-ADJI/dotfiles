# ROADMAP

- [ ] remove redundant default options from large configs.
- [ ] Test Macos setup
- [ ] NixOS — scaffold `nixos/` with flake, host config, Home Manager, system/home modules (follow same structure as `macos/`)
